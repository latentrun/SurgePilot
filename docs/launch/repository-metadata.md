# GitHub Repository Metadata

Apply these values manually only after `latentrun/SurgePilot` is public and every URL has passed an
anonymous check. This file is the review source; it does not change GitHub settings.

## About description

```text
A self-hosted distributed API load-testing system—and an inspectable original baseline built end-to-end by AI agents.
```

This describes the artifact and authorship hook without claiming production adoption, benchmark
results, hosted service availability, or a current Star count.

## Website

```text
https://latentrun.github.io/SurgePilot/
```

## Topics

Use this ordered set and do not add unrelated trend terms:

```text
ai-agents
agentic-engineering
load-testing
performance-testing
distributed-systems
self-hosted
api-testing
developer-tools
docker
fastapi
react
```

## Social preview

- Source file: `docs/site/public/surgepilot-architecture.jpg`
- Required dimensions: 1200×630 JPEG
- Approved message: the AI-authorship hook plus visible repository/system evidence
- Upload path: **Settings → General → Social preview → Edit**

After upload, use a logged-out share debugger or raw Open Graph fetch to confirm the current image,
not a cached prior asset. The uploaded GitHub repository preview and the Pages `og:image` should use
the same reviewed source image.

## Feature settings

- Issues: enabled at launch for reproducible bugs and bounded feature proposals.
- Private vulnerability reporting: enabled before public visibility.
- Wiki: disabled; public user guidance belongs at the Website above.
- Sponsorships: disabled unless the owner makes a separate funding decision.
- **GitHub Discussions: disabled at launch**; enable later only with a moderation and response plan.
- Projects: disabled unless a public roadmap is deliberately maintained.

## Manual verification

- [ ] About description fits without truncation and matches the README claim boundary.
- [ ] Website resolves to `https://latentrun.github.io/SurgePilot/` anonymously.
- [ ] Topics are spelled exactly, relevant, and free from keyword stuffing.
- [ ] Social preview is sharp at desktop and mobile share sizes.
- [ ] License is detected as MIT and community/security files appear in GitHub's UI.
- [ ] Releases and Packages are visible anonymously before any launch draft links to them.
